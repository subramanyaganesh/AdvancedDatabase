GROUP 10
========================
1)Subramanya Ganesh
2)Kachikwu Nwike
3)Deshon Langdon 

Steps to Execute the test cases
=========================

1) Clone the project template from git.
2) Navigate to the project assign4
3) Validate that all the files are present using the ls command
4) Run "make clean" command to clear all the old .o or object files
5) Run "make" command to compile the code and create the corresponding objet file
6) Run "make run1" 
7) Run "make run2" 

FUNCTIONS USED
========================

1) initIndexManager (void *mgmtData)
  -This function initializes the Btree.

2) shutdownIndexManager()
  -This function closes index manager

3) createBtree (char *idxId, DataType keyType, int n)
  -This function allocates memory to all Btree struct elements. Creates a page file called idxId.

4) openBtree (BTreeHandle **tree, char *idxId)
  -This function opens the page file with the name idxID.

5) extern RC closeBtree (BTreeHandle *tree)
  -Frees all the allocated memory.

6) deleteBtree (char *idxId)
  -This function deletes all records from the specified Btree.


Btree Information Methods
========================

7) getNumNodes (BTreeHandle *tree, int *result)
  -This function returns the number of Btree nodes.

8) getNumEntries (BTreeHandle *tree, int *result)
  -The total number of entries in a Btree is returned by this function.

9) getKeyType (BTreeHandle *tree, DataType *result)
  -This function returns the key type, such as Integer, String, and so on.


Index Access Methods
========================

10) findKey(BTreeHandle *tree, Value *key, RID *result)
   -This function returns the key that must be searched.

11) insertKey (BTreeHandle *tree, Value *key, RID rid)
   -This function puts the specified key into a Btree.

12) deleteKey (BTreeHandle *tree, Value *key)
   -This function removes the corresponding key from a Btree.

13) openTreeScan (BTreeHandle *tree, BT_ScanHandle **handle)
   -This function searches over all of the tree''s entries.

14) nextEntry (BT_ScanHandle *handle, RID *result)
   -This function reads the Btree''s next entry.

15) closeTreeScan (BT_ScanHandle *handle)
   -This function terminates tree element scanning.